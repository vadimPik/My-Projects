USE DocumentsExam
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

IF NOT EXISTS ( SELECT TOP (1) 1 
				FROM sys.tables AS T
				WHERE T.name = 'Keywords' )
BEGIN

	CREATE TABLE dbo.Keywords(
		KeywordID INT IDENTITY(1,1) NOT NULL,
		Keyword VARCHAR(255) NOT NULL
	)

END 