USE ShopProj
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

IF NOT EXISTS ( SELECT TOP (1) 1 
				FROM sys.tables AS T
				WHERE T.name = 'UserLoginDetails' )
BEGIN

	CREATE TABLE dbo.UserLoginDetails(
		UserID VARCHAR(255) NOT NULL,
		Password VARCHAR(255) NOT NULL
	)

END 